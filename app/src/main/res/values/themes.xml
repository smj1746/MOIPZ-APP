<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Material3 기반 앱 테마 -->
    <style name="Theme.MOIPZApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- (선택) 포인트 컬러 -->
        <item name="colorPrimary">@color/moipz_green_500</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <!-- (선택) 시스템 바 색 -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
